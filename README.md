# Leads Tracker SM

A simple Chrome extension for saving links and the current browser tab.

## Features

- Save a link from the input field
- Save the current tab with one click
- View saved leads in the popup
- Delete all saved leads

## Run It

1. Open `chrome://extensions`
2. Turn on **Developer mode**
3. Click **Load unpacked**
4. Select this project folder
_._
